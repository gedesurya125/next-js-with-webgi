import { ThemeConfigs } from '@gedesurya125/surya-ui';
export const themeConfig = new ThemeConfigs({
  columnGaps: [0.3, 0.5],
  columnAmounts: [12, 24],
  containerWidths: [35, 100],
  growRatio: 8.5,
  normalizedRemValue: 10,
  breakpoints: ['320px', '1000px'],
  targetScreenSizes: [375, 1194]
});
